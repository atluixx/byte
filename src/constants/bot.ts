const root = process.cwd();

export const AUTH_DIR = `${root}/src/core/socket/auth`;
export const TEMP_DIR = `${root}/temp`;
export const LOG_DIR = `${root}/src/logs`;

export const STORES_DIR = `${root}/src/stores`;
export const NAMES_FILE = `${STORES_DIR}/names.json`;
export const CACHE_FILE = `${STORES_DIR}/cache.json`;

export const COMMANDS_FOLDER = `${root}/src/commands`;
